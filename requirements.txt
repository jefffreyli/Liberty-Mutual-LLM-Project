# Data generation
openai>=1.0.0
pydantic>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
datasets>=2.0.0

# Training (pulls in the tinker SDK, torch, and transformers)
tinker-cookbook>=0.5.0

# Metrics dashboard. Optional: without WANDB_API_KEY the trainers still write
# metrics.jsonl and simply skip the upload.
wandb>=0.18.0
