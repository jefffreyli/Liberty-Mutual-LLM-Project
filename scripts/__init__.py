"""Command line entry points. Each module here is thin: it parses arguments and calls into src/.

Every script runs as a module from the repository root, for example:

    python3 -m scripts.generate
"""
